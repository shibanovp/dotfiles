# dagger installs in a /bin folder.
dagger_path: /usr/local
dagger_release_version: v0.10.3 # renovate: datasource=github-releases depName=dagger lookupName=dagger/dagger versioning=docker
dagger_version: "{{ dagger_release_version[1:] }}"
